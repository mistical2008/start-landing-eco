{
  "name": "parcel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "css:fix": "npx stylelint --fix 'src/**/*.css'",
    "start": "npx rimraf dist .cache & npx parcel ./src/pug/index.pug --open",
    "build": "npx rimraf dist .cache & npx parcel build ./src/pug/index.pug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.18.0",
    "eslint-config-prettier": "^7.2.0",
    "parcel-plugin-stylelint-all": "^0.2.0",
    "postcss-flexbugs-fixes": "^5.0.2",
    "postcss-import": "^14.0.0",
    "postcss-nested": "^5.0.3",
    "postcss-preset-env": "^6.7.0",
    "prettier": "^2.2.1",
    "rimraf": "^2.7.1",
    "stylelint-config-prettier": "^8.0.2",
    "stylelint-config-rational-order": "^0.1.2"
  },
  "dependencies": {
    "sanitize.css": "^12.0.1"
  }
}
